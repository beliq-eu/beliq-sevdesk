node_modules
dist
*.log
.env
.env.*
!.env.example
.DS_Store
coverage

# The worker's runtime output: the persisted high-water-mark and the converted
# documents it writes. Never commit these.
.beliq-sevdesk-state.json
out/

# Local SDK tarball used to verify against the published package shape before
# @beliq/sdk is on npm.
*.tgz
